#!/usr/bin/env bash
set -euo pipefail

# Apply the Brewfile. Idempotent.
if ! command -v brew &>/dev/null; then
    eval "$(/opt/homebrew/bin/brew shellenv)"
fi

brew bundle --file="$DEV_ENV/Brewfile"
