#!/usr/bin/env bash
set -euo pipefail

# Install uv and latest stable Python. Idempotent.
if ! command -v uv &>/dev/null; then
    curl -LsSf https://astral.sh/uv/install.sh | sh
fi

uv self update
uv python install --default latest
